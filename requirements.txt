# Demanding Strongmen — Python dependencies
# Install: pip install -r requirements.txt

# Data I/O
pyreadstat>=1.3.3       # SPSS .sav file reading (WVS Trend, WVS Wave 7)
pyspssio>=0.5.1         # IBM SPSS I/O module wrapper (EVS Trend — handles malformed MRSETS)
pandas>=2.0.0
pyarrow>=14.0.0         # Parquet I/O
fastparquet>=2023.0     # Alternative Parquet backend

# Statistical analysis
numpy>=1.24.0
scipy>=1.11.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# Multilevel models / mixed effects
linearmodels>=5.3       # Panel models (entity FE, RE, Hausman-Taylor)

# Survival / hazard analysis
lifelines>=0.27.0       # Cox PH and discrete-time hazard models

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0            # Progress bars for long-running operations
